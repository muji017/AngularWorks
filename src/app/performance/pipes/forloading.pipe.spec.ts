import { ForloadingPipe } from './forloading.pipe';

describe('ForloadingPipe', () => {
  it('create an instance', () => {
    const pipe = new ForloadingPipe();
    expect(pipe).toBeTruthy();
  });
});
